{
  "name": "apps-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx src/index.ts",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@job-scraper/database": "*",
    "@job-scraper/notifier": "*",
    "@job-scraper/scraper-core": "*",
    "@job-scraper/shared": "*",
    "node-cron": "^4.5.0",
    "pdf-parse": "^2.4.5",
    "tsx": "^4.19.2"
  },
  "devDependencies": {
    "@types/node-cron": "^3.0.11"
  }
}
